<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.knowledge.questioncard.mapper.BankFavoriteMapper">

    <resultMap id="BaseResultMap" type="com.knowledge.questioncard.entity.BankFavorite">
        <id column="id" property="id" />
        <result column="user_id" property="userId" />
        <result column="bank_id" property="bankId" />
        <result column="created_at" property="createdAt" />
    </resultMap>

    <insert id="insert" parameterType="com.knowledge.questioncard.entity.BankFavorite" 
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO bank_favorites (user_id, bank_id, created_at)
        VALUES (#{userId}, #{bankId}, #{createdAt})
    </insert>

    <delete id="deleteByUserAndBank">
        DELETE FROM bank_favorites 
        WHERE user_id = #{userId} AND bank_id = #{bankId}
    </delete>

    <select id="selectByUserAndBank" resultMap="BaseResultMap">
        SELECT * FROM bank_favorites 
        WHERE user_id = #{userId} AND bank_id = #{bankId}
    </select>

    <select id="selectBankIdsByUser" resultType="long">
        SELECT bank_id FROM bank_favorites 
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
    </select>

    <select id="countByBank" resultType="long">
        SELECT COUNT(*) FROM bank_favorites 
        WHERE bank_id = #{bankId}
    </select>

</mapper>